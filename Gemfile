source "https://rubygems.org"

# Use the same dependency set GitHub Pages uses in production.
gem "github-pages", group: :jekyll_plugins

# Required for `bundle exec jekyll serve` on modern Ruby.
gem "webrick", "~> 1.8"
